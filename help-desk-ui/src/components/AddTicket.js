import React from 'react'
import axios from 'axios'

function AddTicket() {
  const {title, id, email } = this.this.state;
  
  if (title === "") {
    
  }
  return (
    
  
  )
}

export default AddTicket